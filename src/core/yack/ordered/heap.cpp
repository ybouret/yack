
#include "yack/ordered/heap.hpp"

namespace yack
{
    namespace low_level
    {
        heap:: ~heap() noexcept
        {
        }

        heap:: heap() noexcept
        {
        }

        const char * heap:: category() const noexcept { return "heap"; }
    }
}
